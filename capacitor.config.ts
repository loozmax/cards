import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cards.game',
  appName: 'Cards',
  webDir: 'dist/cards',
  bundledWebRuntime: false
};

export default config;
