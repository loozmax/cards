import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.durakbluetooth',
  appName: 'Дурак Bluetooth',
  webDir: 'dist/durak-bluetooth',
  bundledWebRuntime: false
};

export default config;
